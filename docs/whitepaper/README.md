# Recce White Paper

Source:

```bash
docs/whitepaper/recce_whitepaper.tex
```

Screenshot asset:

```bash
docs/whitepaper/assets/recce-packet-workflow.png
```

Compile from this directory with a LaTeX engine:

```bash
cd docs/whitepaper
pdflatex recce_whitepaper.tex
pdflatex recce_whitepaper.tex
```

The paper documents the system architecture, API workflow, test matrix,
Kubernetes deployment path, and starter guide for running Recce locally, with
Docker, and on Kubernetes.
